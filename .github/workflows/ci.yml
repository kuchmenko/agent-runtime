name: CI

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  checks:
    uses: ./.github/workflows/ci-checks.yml
    permissions:
      contents: read
      security-events: write
    secrets: inherit
